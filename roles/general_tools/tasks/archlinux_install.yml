---

- name: Install general tools...
  become: true
  community.general.pacman:
    name: "{{ item }}"
    state: present
  loop:
    - git
    - bat
    - htop
    - unzip
    - firefox
    - obsidian
    - keepassxc
    - curl
    - gcc
    - code
    - make
    - mypaint
    - discord
    - timeshift
    - arandr
    - okular
    - gedit
  tags: general_tools_install, install

...
