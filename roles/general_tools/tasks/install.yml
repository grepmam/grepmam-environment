---

- name: Install general tools...
  become: true
  community.general.pacman:
    name: "{{ item }}"
    state: present
  loop:
    - bat
    - htop
    - unzip
    - firefox
    - obsidian
    - keepassxc

...
