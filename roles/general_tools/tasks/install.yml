---

- name: Install general tools...
  become: true
  community.general.pacman:
    name: "{{ item }}"
    state: present
  with_items:
    - bat
    - htop
    - unzip
    - firefox
    - obsidian
    - keepassxc

...
