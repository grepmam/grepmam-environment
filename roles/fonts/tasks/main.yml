---

- name: Include install tasks...
  ansible.builtin.import_tasks: install.yml
  tags: fonts_install, install

- name: Import configure.yml tasks... 
  ansible.builtin.import_tasks: configure.yml
  tags: fonts_configure, configure

...
