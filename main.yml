---

- name: Grepmam Environment
  hosts: localhost_group
  gather_facts: false

  tasks:

    - name: Include tasks from i3 role (install)...
      ansible.builtin.include_role:
        name: i3
        tasks_from: install

    - name: Include tasks from i3 role (configure)...
      ansible.builtin.include_role:
        name: i3
        tasks_from: configure

...
